
  # OrderFlow

  This is a code bundle for OrderFlow. The original project is available at https://www.figma.com/design/DifHsnECAPNkffwvC6eqdf/OrderFlow.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  